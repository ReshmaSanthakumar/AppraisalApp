App As appinfo:
    BackEnabled: =true
    OnStart: |
        =If(
            Not(IsBlank(
                LookUp( AppraisalAdminList,
                   
                    Title = User().Email
                )
            )),
            Set(
                is_admin,
                true
            ),
            Set(
                is_admin,
                false
            )
        );
        Set(col1,AppraisalUserList);
        Set(varPageNumber,1)
    StartScreen: =Main_screen
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

